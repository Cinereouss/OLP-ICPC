#include <bits/stdc++.h>

using namespace std;

int N;

int main(){
	freopen("WATER.INP", "r", stdin);
	//freopen("WATER>OUT", "w", stdout);
	cin >> N;
	int arr[N][2];
	arr[N][2];
	for(int i = 0; i < N; i++){
		cin >> arr[i][0] >> arr[i][1];
		cout << arr[i][0] <<" "<< arr[i][1] << endl;
	}
}
